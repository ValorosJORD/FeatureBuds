info:
  name: GetAllProjects
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:9812/projects
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
