info:
  name: LogoutUser
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:9812/sessions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
